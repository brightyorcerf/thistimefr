name: inner_child
description: A gamified emotional accountability companion — "Studio Ghibli meets Sanrio."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.2.1
  rive: ^0.13.0
  flutter_animate: ^4.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  # Add your rive file to assets once you have it:
  # assets:
  #   - assets/rive/mascot.riv
  #   - assets/images/
